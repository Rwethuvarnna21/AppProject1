import react from 'react';
import { View,Button,Text,Modal } from 'react-native';
const Modals=({visible, item, onCancel, onConfirm })
return(
    <Modal animationType='slide' transparent={true} visible={visible}>

    </Modal>
)